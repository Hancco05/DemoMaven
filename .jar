# Usa una imagen base de Tomcat, porque tu proyecto es un WAR
FROM tomcat:9.0-jdk11

# Elimina la aplicación predeterminada de Tomcat
RUN rm -rf /usr/local/tomcat/webapps/*

# Copia el archivo WAR generado por Maven al directorio de despliegue de Tomcat
COPY target/DemoMaven-1.0-SNAPSHOT.war /usr/local/tomcat/webapps/ROOT.war

# Expone el puerto 8080 para acceder a la aplicación
EXPOSE 8080

# Comando para iniciar Tomcat
CMD ["catalina.sh", "run"]
